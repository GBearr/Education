import React from 'react';
import {Text, View} from 'react-native';
const BiologyScreen = ({item}) => {
  console.log(item);
  return (
    <View>
      <Text>Biyoloji</Text>
    </View>
  );
};

export default BiologyScreen;
